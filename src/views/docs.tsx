import { Typography } from "@material-ui/core";

function Docs() {
  return (
    <Typography
      style={{
        display: "grid",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      This is the Docs page
    </Typography>
  );
}

export default Docs;
